import React from 'react'
import ReactDOM from 'react-dom/client'
import Katora from './components/katora'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Katora />
  </React.StrictMode>,
)
